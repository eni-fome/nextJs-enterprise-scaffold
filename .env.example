# ==============================================================================
# Environment Configuration
# ==============================================================================
# Copy this file to .env and fill in the values.
# See src/env.ts for the full schema and validation rules.
# ==============================================================================

# Server-side (not exposed to browser)
NODE_ENV=development
# DATABASE_URL=postgresql://user:pass@localhost:5432/mydb
# API_SECRET_KEY=your-secret-key-min-32-chars

# Client-side (exposed to browser - prefix with NEXT_PUBLIC_)
# NEXT_PUBLIC_API_URL=http://localhost:3000/api
NEXT_PUBLIC_APP_NAME="NES App"
